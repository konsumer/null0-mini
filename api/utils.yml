trace:
  description: print a string
  returns: void
  args:
    str: string

abort:
  description: a fatal exit
  returns: void
  args:
    message: string
    filename: string
    line: u32
    column: u32
